table
    thead
      tr 
        th Completed
        th Task Name
    tbody  
      - var i = 0
      each task in tasks
        tr
          - i++
          td
            label
              input(id='check' type='checkbox' onChange="postByFetch('/task/update', "+task.id +",this.checked)")
          td #{task.title}
